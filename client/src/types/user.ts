export type User = {
  _id: string;
  username: string;
  email: string;
  password: string;
  profilePicture: string;
  isAdmin: boolean;
  // products: Product[];
  // comments: Comment[];
  // replies: Reply[];
}